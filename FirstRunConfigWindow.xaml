<Window x:Class="FirstRunConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BetterNTE · 首次配置"
        Width="520"
        Height="320"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#0A0E17">
    <Border Padding="28" Background="#0A0E17">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="欢迎使用 BetterNTE"
                           Foreground="White"
                           FontSize="22"
                           FontWeight="SemiBold" />
                <TextBlock Margin="0,8,0,0"
                           Text="请选择异环游戏安装目录"
                           Foreground="#9CA3AF"
                           FontSize="14" />
            </StackPanel>

            <Grid Grid.Row="2" Margin="0,34,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="PathTextBox"
                         Grid.Row="0"
                         Grid.Column="0"
                         Height="38"
                         Padding="10,0"
                         VerticalContentAlignment="Center"
                         Background="#111827"
                         BorderBrush="#374151"
                         Foreground="#E5E7EB"
                         CaretBrush="#E5E7EB"
                         TextChanged="PathTextBox_TextChanged" />

                <Button Grid.Row="0"
                        Grid.Column="1"
                        Width="92"
                        Height="38"
                        Margin="12,0,0,0"
                        Content="浏览..."
                        Click="BrowseButton_Click" />

                <TextBlock x:Name="ValidationTextBlock"
                           Grid.Row="1"
                           Grid.ColumnSpan="2"
                           Margin="2,10,0,0"
                           Foreground="#F87171"
                           FontSize="12"
                           Text="请选择包含 NTEGame.exe 的游戏目录" />
            </Grid>

            <StackPanel Grid.Row="3"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <Button Width="86"
                        Height="34"
                        Content="取消"
                        Click="CancelButton_Click" />
                <Button x:Name="ConfirmButton"
                        Width="86"
                        Height="34"
                        Margin="12,0,0,0"
                        Content="确定"
                        IsEnabled="False"
                        Click="ConfirmButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
